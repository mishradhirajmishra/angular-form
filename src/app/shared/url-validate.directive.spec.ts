import { UrlValidateDirective } from './url-validate.directive';

describe('UrlValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlValidateDirective();
    expect(directive).toBeTruthy();
  });
});
